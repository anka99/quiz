class State {
    constructor(currentQuestion, renderMode) {
        this.currentQuestion = currentQuestion;
        this.renderMode = renderMode;
    }
}
export default State;
//# sourceMappingURL=State.js.map